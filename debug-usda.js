// Debug USDA API nutrient structure\nasync function debugUSDA() {\n  const food = 'raw apple';\n  \n  console.log('Debugging USDA API nutrient structure for:', food);\n  \n  try {\n    const url = `https://api.nal.usda.gov/fdc/v1/foods/search?query=${encodeURIComponent(food)}&pageSize=1&api_key=DEMO_KEY`;\n    const response = await fetch(url);\n    \n    if (response.ok) {\n      const data = await response.json();\n      \n      if (data.foods && data.foods.length > 0) {\n        const foodItem = data.foods[0];\n        console.log('Description:', foodItem.description);\n        console.log('Total nutrients:', foodItem.foodNutrients ? foodItem.foodNutrients.length : 'undefined');\n        \n        if (foodItem.foodNutrients) {\n          console.log('\\n=== First 10 Nutrients (Debug) ===');\n          foodItem.foodNutrients.slice(0, 10).forEach((nutrient, index) => {\n            console.log(`${index + 1}.`);\n            console.log('  Full nutrient object:', JSON.stringify(nutrient, null, 2));\n            console.log('');\n          });\n        }\n      }\n    }\n  } catch (error) {\n    console.log('Error:', error.message);\n  }\n}\n\ndebugUSDA();